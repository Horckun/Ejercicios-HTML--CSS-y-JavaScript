function funcion() {
  // Contador de número de carácteres
  var frase = "Pero no hay queja del que deja y no se enoja por las tejas que caen del tejado agotadas por viejas"
  var contar = frase.length;
  document.write("La frase: "+frase+" tiene "+contar+" carácteres.");
}
