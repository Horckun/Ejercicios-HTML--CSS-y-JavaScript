function funcion() {
  // Creación de Array
  var cadena = new Array(8);
  // Medición de Array
  var tamano = cadena.length;
  // Mostrar array
  document.write("El array tiene "+tamano+" elemento");
}
