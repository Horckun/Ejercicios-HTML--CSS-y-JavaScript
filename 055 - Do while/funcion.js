// Declaramos variable
var x = 8;
var numero;

// Solicitamos número
do {
  numero = prompt("Introduce un valor entre el 0 y el 10:");
} while(numero != 8);

// Verificamos número
if(numero == x) {
  alert("Has acertado");
}
